<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" type="text/css" href="stylesheets/semantic.css">
  <script src="javascripts/jq.js"></script>
  <script src="javascripts/semantic.min.js"></script>
  <style>
    body {
      background: #FCFCFC url(../images/bg.jpg) repeat;
    }
  </style>
</head>
<body>
  <div class="ui page grid">
    <div class="ui column">
      <div class="ui header">
        <h1><%= title %></h1>
        <div class="content">Welcome to <%= title %></div>
        <div class="sub header">这里是一个测试的树洞，暂时木有各种样式表</p>
        </div>

        <div class="ui red message">
          <div class="header">
            公告
          </div>
          <p>
            请注意：红色是隐藏，绿色是恢复。请务必珍惜您的权限！
          </p>
        </div>
      </div>

      <%  for (i=wishes.length-1; i>=0; i--) { %>
      <div class="ui piled segment">
        <% if (wishes[i].visible){ %>
        <form action="" method="post">
          <button class="small red icon ui left floated button" type="submit" value='<%= wishes[i].id %>' name="deleteId">
           <i class="remove icon"></i>
         </button>
       </form>
       <%  };%>
       <% if (!wishes[i].visible){ %>
       <form action="" method="post">
        <button class="small green icon ui left floated button" type="submit" value='<%= wishes[i].id %>' name="restartId">
         <i class="add icon"></i>
       </button>
     </form>
     <%  };%>
     <h3 class="ui left floated header ">第<%= wishes[i].id%>条消息  <%=wishes[i].from %> 对 <%=wishes[i].to %> 说：</h3>
     <h3 class="ui right floated header"><%=wishes[i].timeS%></h3>
     <div style="clear: both"></div>
     <p><%= wishes[i].content%></p>
   </div>
   <%  };%>
 </div>
</div>

</div>
</body>
</html>
